/**
 * arr = [3,4,5,1,2]
 * 一个数的父节点是它左边比它大的数和它右边比它大的数中，较小的那一个。
 * 如果一个数的左边和它的右边都没有比它大的数，那么就说明这个数是最大的，是二叉树的根结点。
 */
function Node(val) {
    this.val = val;
    this.left = null;
    this.right = null;
}
function MaxTree(arr) {
    var node = new Node();
    for (let i = 0; i < arr.length; i++) {
        // if()
    }
}
